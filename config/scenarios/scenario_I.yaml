# =============================================================================
# Scenario I: Balanced Load（平衡负载场景）
# =============================================================================
# 配置: S+L Clients (QPM=50)
# 描述: Balanced Load
# 客户端: 4 (2S+2L)
# QPM: 50, 50, 50, 50
# SLO: 20, 30
# =============================================================================

name: "Scenario_I_Balanced_Load"
description: "Balanced Load with 2 Short + 2 Long clients"

clients:
  short:
    count: 2
    qpm: [50, 50]
    qpm_ratio: 1
    slo: [20, 30]
  
  long:
    count: 2
    qpm: [50, 50]
    qpm_ratio: 1
    slo: [20, 30]
  
  mix:
    count: 0
    qpm: []
    qpm_ratio: 1
    slo: []

# 总QPM: 200
